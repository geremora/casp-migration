const OFFSET_TBL_CITACIONES = 740000;


//Option to match id (+/-) 100,000

const MEETINGS_TYPE = {
    UNASIGNED: 1,
    ARBITRAJE: 2
};

const MEETINGS_OFFSET_ID = {
    OFFSET_TBL_CITACIONES: OFFSET_TBL_CITACIONES
};

module.exports.MEETINGS_OFFSETS_ID = MEETINGS_OFFSET_ID;
module.exports.MEETINGS_TYPE = MEETINGS_TYPE;